name: Executar Scraper Agrícola en Tiempo Real

on:
  schedule:
    # Se ejecuta cada 30 minutos de Lunes a Viernes entre 12:00 y 21:00 UTC
    - cron: '*/30 12-21 * * 1-5'
  workflow_dispatch: # Disparo manual desde la pestaña Actions

# Permisos explícitos de escritura para que el bot pueda hacer push
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del Repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js (v20)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar Dependencias
        run: |
          npm init -y
          npm install axios cheerio

      - name: Ejecutar Script Scraper
        run: node scripts/scraper.js

      - name: Commit y Push de Datos Actualizados
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/pizarra.json
          git diff-index --quiet HEAD || git commit -m "chore(data): actualización en vivo [skip ci]"
          git push
